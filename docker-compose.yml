version: '3'

services:
  web:
    build: .
    command: gunicorn --workers 3 --bind 0.0.0.0:8000 guichet.wsgi:application
    volumes:
      - .:/code
    env_file:
      - .env

  nginx:
    build: ./nginx
    ports:
      - "80:80"
    depends_on:
      - web
